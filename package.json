{
  "name": "sqm-style-quality-model",
  "version": "2.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env.development.local server.js",
    "start": "node --env-file-if-exists=.env.development.local server.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1",
    "formidable": "^3.5.4"
  }
}
